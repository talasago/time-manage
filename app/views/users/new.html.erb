<% provide(:title, 'ユーザー新規登録') %>
<% provide(:url, new_user_path) %>
<% provide(:button_text, '利用規約に同意の上、ユーザを作成する') %>
<div class="wrapper">
  <h1>ユーザー新規登録</h1>
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <%= render 'form' %>
    </div>
  </div>
</div>
